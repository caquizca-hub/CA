# App Icon

Please place your app icon image here and name it `icon.png`.
Recommended size: 1024x1024 pixels.

Once you have added the image, run the following command in the terminal to generate the icons for all platforms:

```bash
dart run flutter_launcher_icons -f flutter_launcher_icons.yaml
```
